# MSSQL server / credentials
MSSQL_SERVER=10.0.66.77\MSSQL2019
MSSQL_PORT=1433
MSSQL_DATABASE=EmpBook_db

# ถ้าใช้ SQL Server Authentication
MSSQL_USER=sa
MSSQL_PASSWORD=Systemadmin@!

# หรือถ้าต้องการใช้ Windows Integrated Authentication (Kerberos) บนเครื่องที่ตั้งค่าไว้
# USE_TRUSTED_CONNECTION=true

# ชื่อ ODCBC driver ที่ติดตั้งบนระบบของคุณ (ตรวจสอบด้วย pyodbc.drivers())
MSSQL_ODBC_DRIVER=ODBC Driver 18 for SQL Server

# ถ้าใช้ cert self-signed ใน dev ให้ตั้ง yes เพื่อไม่ให้ TLS fail
MSSQL_TRUST_SERVER_CERT=yes

# Docker Compose build performance options
# Enable delegation to Buildx Bake for faster builds
COMPOSE_BAKE=true

# Optional speed-ups (uncomment if desired)
# DOCKER_BUILDKIT=1
# COMPOSE_DOCKER_CLI_BUILD=1
