# ZKTeco Attendance System

ระบบจัดการเวลาเข้า-ออกงานจากเครื่องสแกนนิ้ว/ใบหน้า ZKTeco แบบ Real-time ประกอบด้วยบริการหลัก 2 ส่วนที่ทำงานร่วมกัน เพื่อความถูกต้องและสมบูรณ์ของข้อมูล

---

## 🏗️ โครงสร้างระบบ (Architecture)

ระบบถูกแยกออกเป็น 2 บริการอิสระ เพื่อประสิทธิภาพและความเสถียร:

1.  **Attendance Listener (`zkteco_listener.py`)**:
    - ทำหน้าที่รับข้อมูลการสแกนแบบ Real-time และบันทึกลงฐานข้อมูล SQL Server ทันที
    - เปรียบเสมือน **"รปภ. หน้าประตู"** ที่เช็คว่า _"อ้าว... เมื่อวานคุณยังไม่ได้แสกนออกเลยนะ เดี๋ยวผมบันทึกย้อนหลังให้ตอนนี้เลย ก่อนที่จะเริ่มเข้ากะใหม่"_
2.  **Cleanup Service (`cleanup_service.py`)**:
    - บริการหลังบ้านสำหรับตรวจสอบและปิด Record ที่พนักงานลืมสแกนออก (Incomplete Records)
    - เปรียบเสมือน **"ภารโรงตอนดึก"** ที่มาเดินตรวจตอนที่ไม่มีคนอยู่ แล้วช่วยปิดไฟ/ปิดงานให้สำหรับคนที่ลืมทิ้งไว้ครับ

---

## 📡 1. Attendance Listener (Real-time Capture)

ทำหน้าที่เชื่อมต่อกับเครื่อง ZKTeco ทุกเครื่องในเน็ตเวิร์กผ่านระบบ **Live Capture** โดยแยกการทำงานเป็น 1 Thread ต่อ 1 เครื่อง

### กระบวนการบันทึกข้อมูล (Shift-based Logic)

ระบบจะตรวจสอบกะงานของพนักงานจาก `[db_pfpdashboard].[dbo].[VListPeriodEmployee]` เพื่อตัดสินใจ:

- **บันทึกการสแกนเข้า (Check-In)**: แสกนในช่วงครึ่งแรกของกะงาน
- **บันทึกการสแกนออก (Check-Out)**:
  - แสกนในช่วงครึ่งหลังของกะงาน
  - มีข้อมูลสแกนเข้าในกะเดิมอยู่แล้ว
  - มีการแสกนซ้ำภายใน 1 ชั่วโมงหลังจากแสกนออกล่าสุด (Update เวลาออกล่าสุด)
- **การปิดกะเก่าอัตโนมัติ (Safety Cleanup)**:
  - หากพนักงานแสกนกะใหม่ แต่กะเดิมยังไม่มีการแสกนออก ระบบจะดึงเวลาเลิกงานตามแผนของกะเดิมมาปิดให้ทันที (Stamp เป็น `AUTO_CLEANUP`) ก่อนเริ่มบันทึกข้อมูลกะใหม่

### ความเสถียร (Reliability)

- **Auto Reconnect**: หากการเชื่อมต่อหลุด ระบบจะพยายามเชื่อมต่อใหม่ทุก 30 วินาที
- **Normalize ID**: บังคับรหัสพนักงานเป็น 5 หลักเสมอ (เช่น `5233` -> `05233`)

---

## 🧹 2. Cleanup Service (Standalone Service)

บริการที่รันแยกเพื่อจัดการ Record ที่ค้างอยู่ในระบบ (TimeOut IS NULL) โดยเฉพาะกรณีที่พนักงานไม่ได้กลับมาแสกนอีกเลยจนข้ามวัน

### เงื่อนไขการทำงาน

- **ความถี่ในการทำงาน**: ตรวจสอบทุกรอบ (ตั้งค่าได้ผ่าน `CLEANUP_INTERVAL_SECONDS`)
- **เกณฑ์การ Cleanup**: ค้นหา Record ที่ไม่มีเวลาออกและเก่าเกิน 16 ชั่วโมง (`CLEANUP_THRESHOLD_HOURS`)
- **Logic การดึงเวลาเลิกงาน**:
  - ค้นหาข้อมูลกะงาน (`InTmp`, `OutTmp`) จากฐานข้อมูลแผนงาน
  - **กรองวันหยุด**: ระบบจะข้าม Record ที่เป็นวันหยุด (`HoliDay=1` และ `InTmp=00:00`) เพื่อค้นหากะงานจริงที่พนักงานเข้าทำงาน
  - **รองรับกะข้ามคืน**: หากเวลาเลิกงานตามกะน้อยกว่าเวลาเข้า จะทำการปรับวันที่ให้เป็นวันถัดไปโดยอัตโนมัติ
  - **Fallback**: หากไม่พบแผนกะงาน จะใช้เวลาเดียวกับเวลาสแกนเข้า (Duration = 0) เพื่อให้ Record สมบูรณ์

---

## ข้อมูลในฐานข้อมูล (Storage)

ข้อมูลจะถูกเก็บลงในตาราง `[EmpBook_db].[dbo].[TimeAttandanceLog]` โดยมีฟิลด์สำคัญดังนี้:

| ฟิลด์           | รายละเอียด                                          |
| :-------------- | :-------------------------------------------------- |
| `DateTimeStamp` | วันที่ที่อ้างอิงกับกะงาน (DatePeriod)               |
| `EmpId`         | รหัสพนักงาน (5 หลัก)                                |
| `TimeIn`        | เวลาที่สแกนเข้าจริง                                 |
| `TimeOut`       | เวลาที่สแกนออกจริง หรือเวลาจากการ Cleanup           |
| `IPStampIn`     | IP ของเครื่องสแกนที่เข้า                            |
| `IPStampOut`    | IP ของเครื่องสแกนที่ออก หรือระบุเป็น `AUTO_CLEANUP` |

---

## 🚀 การติดตั้งและใช้งาน (Deployment)

### 1. การติดตั้งแบบปกติ (Local Development)

1.  ติดตั้ง Library ที่จำเป็น: `pip install -r requirements.txt`
2.  ตั้งค่า Environment Variables ในไฟล์ `.env`
3.  รัน Listener สำหรับรับข้อมูล Real-time: `python zkteco_listener.py`
4.  รัน Cleanup Service สำหรับจัดการข้อมูลค้าง: `python cleanup_service.py`

### 2. การติดตั้งด้วย Docker (Recommended)

แนะนำให้ใช้งานผ่าน Docker เพื่อความสะดวกในการจัดการทรัพยากรและความเสถียร

**เตรียมความพร้อม:**

- ตรวจสอบว่ามีไฟล์ `.env` พร้อมข้อมูลการเชื่อมต่อฐานข้อมูล
- ติดตั้ง Docker และ Docker Compose

**คำสั่งการใช้งาน:**

```powershell
# 1. Build และ Start บริการทั้งหมด (Background)
docker compose up -d --build

# 2. ตรวจสอบสถานะการทำงาน
docker compose ps

# 3. ดู Log การทำงานของ Listener
docker compose logs -f zkteco-listener

# 4. ดู Log การทำงานของ Cleanup Service
docker compose logs -f cleanup-service

# 5. หยุดการทำงาน
docker compose down
```

---

## ⚙️ การตั้งค่าระบบ (Configuration)

ตั้งค่าผ่านไฟล์ `.env` หรือกำหนดใน `docker-compose.yml`:

| ตัวแปร                    | รายละเอียด                         | ค่าเริ่มต้น                     |
| :------------------------ | :--------------------------------- | :------------------------------ |
| `MSSQL_SERVER`            | IP/Hostname ของ SQL Server         | -                               |
| `MSSQL_DATABASE`          | ชื่อฐานข้อมูล (`EmpBook_db`)       | -                               |
| `CLEANUP_THRESHOLD_HOURS` | เกณฑ์เวลา Record ที่ค้าง (ชั่วโมง) | `16`                            |
| `MSSQL_ODBC_DRIVER`       | Driver สำหรับเชื่อม SQL Server     | `ODBC Driver 18 for SQL Server` |

---

_หมายเหตุ: ระบบทั้งสองส่วนออกแบบมาให้ทำงานประสานกัน โดยที่ `zkteco_listener.py` จะจัดการกะงานปัจจุบัน และ `cleanup_service.py` จะจัดการข้อมูลตกค้างในภาพรวม_
